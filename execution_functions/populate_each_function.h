#ifndef _POPULATE_EACH_FUNCTION_H
#define _POPULATE_EACH_FUNCTION_H

#include "../structures.h"

void populate_each_function(struct Value *, struct Value *, struct Tree *, struct Value *, struct Value *);

#endif
